# 2026-07-15 — Phase 4: Live View

## Done
- `Live.tsx`: style switcher, legend, totals (selections + voters), realtime via
  `useSummit`; applies admin default style once on load.
- Four styles: `Bubbles.tsx` (d3-hierarchy pack, dynamic import),
  `Treemap.tsx` (d3 treemap, dynamic import), `BarRace.tsx` (CSS bars),
  `Leaderboard.tsx` (ranked list).
- Click any unit → detail drawer with the people who chose it
  (name + email + country + logo).
- All color-coded by group.

## Verified
Seeded 97 random selections; bubbles/treemap/bar all sized correctly; drawer
listed 12 people for the top unit. Screenshots in session.

## Notes
- d3 chunks are lazy-loaded; keep them dynamically imported.
- Zero-count labs get a 0.35 baseline so they stay visible but tiny.
