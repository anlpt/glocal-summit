# 2026-07-15 — Phase 5: Admin CMS + Excel

## Done
- `AdminLogin.tsx`: `admin`/`123` gate (sessionStorage `gs_admin`).
- `Admin.tsx`: tabbed shell (Overview, Groups & Units, Participants, Selections);
  shows Supabase-live vs offline badge.
- `panels/OverviewPanel.tsx`: voting open/close, hero/instructions/default-style
  editing, master Excel export.
- `panels/GroupsLabsPanel.tsx`: group name/color/CRUD, lab CRUD.
- `panels/ParticipantsPanel.tsx`: search, add/edit/delete, logo upload
  (file → dataURL → `org_logo_url`), group assignment.
- `panels/SelectionsPanel.tsx`: per-unit table with people, reset, exports.
- `src/lib/excel.ts`: `exceljs` (dynamic import) → `exportTable` + `exportMaster`
  (Participants / Selections / Lab Counts / Group Counts sheets).

## Fixed
- OverviewPanel content fields were empty on first paint (useState captured
  pre-load ''); keyed each `TextField` to its loaded value to resync.

## Verified
Login, content fields populate, master Excel export produces a valid blob,
participant list + logos render. Screenshots in session.
